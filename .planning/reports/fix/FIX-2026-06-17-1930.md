# Fix Report — 2026-06-17 19:30

**Symptom:** Remaining audit residuals (user chose "Both"): the `xlsx` HIGH dependency vuln, and the 2 ESLint rules previously downgraded to warn.
**Mode:** general (batched — known root causes from the audit)
**Outcome:** fixed

## Feedback Loop
- Initial: `npm audit --omit=dev` = 1 high (`xlsx`) + 2 moderate; `eslint.config.mjs` had `set-state-in-effect` + `no-html-link-for-pages` downgraded to warn.
- Final: `npm audit` = 2 moderate only (HIGH gone); `npm run lint` exit 0 with both rules at **error**; build/tsc/tests green.

## Root Cause + Fix
| Item | Root cause | Fix |
|---|---|---|
| `xlsx` HIGH (prototype pollution + ReDoS) | npm's `xlsx@0.18.5` is unpatched; the fix lives only on SheetJS's own CDN | `npm i xlsx@https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` → now `xlsx 0.20.3`. Same API (`read`/`utils` intact); the only consumer is a dynamic `await import("xlsx")` at `onedriveTools.ts:510`. |
| `react-hooks/set-state-in-effect` ×3 | synchronous setState in `useEffect` bodies (emails:160, files:82, prep:100) | Refactored each effect to the inner-async-function pattern (load on mount, same fetches/states/deps) — no behavior change. |
| `@next/next/no-html-link-for-pages` ×2 | `<a href="/…">` for in-app nav (emails:241, Nav.tsx:18 + mapped links) | Converted internal links to `next/link` `<Link>`, preserving className/aria/children. |
| ESLint rules | were downgraded to warn to land the gate | Restored both to **error** in `eslint.config.mjs` now that source is clean. |

## Files changed
package.json, package-lock.json, eslint.config.mjs, src/app/emails/page.tsx, src/app/files/page.tsx, src/app/prep/page.tsx, src/components/Nav.tsx

## Verification
- `npm run build` — PASS (exit 0)
- `npx tsc --noEmit` — PASS (0 errors)
- `npm run lint` — PASS (exit 0; both target rules at error, 18 warnings remain — no-img-element / no-unused, non-blocking)
- `npm test` — PASS (12 files / 59 tests)
- `npm audit --omit=dev` — `xlsx` HIGH cleared; 2 moderate `postcss` remain (transitive via Next, fix is a breaking Next downgrade — tracked)

## Remaining Notes
- `xlsx` runtime: API-compatible and builds/types clean; full confidence requires parsing a real .xlsx via the agent (not test-covered) — low risk given identical SheetJS API.
- `postcss` moderate ×2: unchanged residual; resolves when Next ships a release bumping postcss.
- 18 lint warnings remain (`no-img-element`, `no-unused-vars`) — LOW polish, non-blocking.
