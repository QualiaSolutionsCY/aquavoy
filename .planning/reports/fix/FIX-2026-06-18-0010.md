# Fix Report — 2026-06-18 00:10

**Symptom:** Re-audit MEDIUM/LOW residuals — a remaining in-app `<a>` (full page reload instead of client transition) and `npm run lint` missing from the README.
**Mode:** general (small, known root cause from the re-audit)
**Outcome:** fixed

## Feedback Loop
- Initial: `grep '<a href="/' src/` → 1 hit (`prep/page.tsx:331`); `grep "lint" README.md` → 0.
- Final: `grep '<a href="/' src/` → none; README lists `npm run lint`; build/tsc/lint/tests green.

## Root Cause
- `src/app/prep/page.tsx:331` — used a raw `<a href="/emails">` for in-app navigation (causes a full page reload, bypassing Next client routing). The earlier audit-fix converted emails + Nav but missed this one. (Footer.tsx was already clean — the re-audit mis-flagged it.)
- `README.md` "Useful scripts" — omitted the `lint` script even though it now exists in package.json.

## Patch
- Files changed: `src/app/prep/page.tsx`, `README.md`
- Change summary: added `import Link from "next/link"` + converted the `/emails` link to `<Link>` (className/style/children preserved); added `npm run lint   # eslint .` to the README scripts block.

## Verification
- `grep '<a href="/' src/` — PASS (no raw in-app links remain)
- `npm run lint` — PASS (exit 0; 18 warnings, 0 errors)
- `npx tsc --noEmit` — PASS (0 errors)
- `npm test` — PASS (12 files / 59 tests)
- `npm run build` — PASS (exit 0)

## Remaining Notes
- `postcss` ×2 moderate (transitive via Next, no non-breaking fix) — unchanged residual, tracked.
- 18 lint warnings (`no-img-element`, `no-unused-vars`) — LOW polish, non-blocking.
