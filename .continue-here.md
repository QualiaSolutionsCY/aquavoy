# 👋 Continue Here — Aquavoy

**For:** Moayad (or whoever picks this up next)
**Last worked:** 2026-06-16 by Fawzi
**Branch to work from:** `main` (everything is merged + pushed)

---

## TL;DR — where we are

**Milestone 1 (Trust & Hardening) is SHIPPED and live** at https://aquavoy.vercel.app.
**Milestone 2 (Agent Depth) is OPEN at Phase 1 — not started.**

Run `/qualia` to confirm, then `/qualia-scope 1` → `/qualia-plan 1` to continue.

```
✓ M1 Trust & Hardening   SHIPPED (auth · encryption at rest · migrations + 30 tests)
◆ M2 Agent Depth         ← YOU ARE HERE, Phase 1 "Durable Memory", status: setup
  P1 Durable Memory · P2 Inline Doc Understanding · P3 Confirm/Undo
○ M3 Operations Polish   (sketched)
○ M4 Handoff             (final)
```

## First: get the repo running locally

```bash
git pull origin main
npm install
```

**You need env vars — they are NOT in git (gitignored).** Pull them from Vercel (you need access to the `qualiasolutionscy` team / `aquavoy` project):

```bash
npx vercel link        # select: qualiasolutionscy → aquavoy
npx vercel env pull .env.local   # pulls ALL prod env incl. the new SESSION_SECRET / OPERATOR_CREDENTIALS / ENCRYPTION_KEY
npm run build && npm test        # confirm green: build passes, 30 tests pass
npm run dev
```

⚠️ **`ENCRYPTION_KEY` is load-bearing** — mailbox passwords + OAuth tokens are AES-256-GCM encrypted under it. Don't change it; `vercel env pull` gives you the live one. Legacy plaintext rows self-heal on next write.

If you need to log in locally: operators are **Wency** / **Jeanette**. Their passwords live in the prod `OPERATOR_CREDENTIALS` (hashed). For local-only testing you can mint your own with the scrypt format in `src/lib/auth/session.ts` (`verifyCredential`), or ask Fawzi for the session passwords.

## What M1 shipped (so you have context)

- **Auth (ADR-001):** `src/proxy.ts` (Next 16 guard) gates every route except `/login`, `/api/login`, and the cron. Principal comes from a signed httpOnly session cookie, read via `src/lib/auth/session.ts` — never from request input. Login page at `src/app/login/page.tsx`.
- **Encryption at rest:** `src/lib/crypto/secrets.ts` (AES-256-GCM, Node built-in). `mail/accounts.ts` + `microsoft/connections.ts` encrypt secrets on write, decrypt at point of use.
- **Migrations + tests:** `supabase/migrations/0007` (scheduled_emails) + `0008` (constraint reconcile); vitest + 30 seam tests (`npm test`).
- Decision record: `.planning/decisions/ADR-001-access-control-strategy.md`. M1 detail archived in `.planning/archive/milestone-1-trust-and-hardening/`.

## M2 Phase 1 — your next task: Durable Memory

**Goal:** recall that works across long histories, not just keyword-grep over recent messages (today's `autoRecall` in `src/lib/agents/memoryTools.ts:61-100` greps for ≥5-char word overlaps — misses paraphrases, drowns in long histories).

**The fork to decide in `/qualia-scope 1` (worth an ADR):** how to upgrade recall —
- **(Recommended) Rolling per-session summarization + salience/recency ranking** — no new infra, fits the "no new dependency" pattern M1 followed.
- Semantic/embedding recall (needs a vector store — heavier).
- Better keyword ranking only (cheapest, least capable).

Keep the dual-path model (server `autoRecall` + callable `recall_memory`) and the M1 invariant: **memory stays scoped to the session principal** (don't reintroduce the cross-principal leak M1 closed).

Full roadmap: `.planning/ROADMAP.md`. Glossary: `.planning/CONTEXT.md`. Product/voice: `.planning/PRODUCT.md`.

## Watch out for

- **Work on a feature branch**, not main (Qualia rule — main stays deployable). `git checkout -b m2-agent-depth`.
- Deploys are **CLI-only** (`vercel --prod`); GitHub auto-deploy is disabled on purpose.
- There's a known framework quirk this session: a spawned planner subagent's notifications looked like state mutations after a process boundary. If state ever looks ahead of reality, trust git + the contract-runner over the cursor; `/qualia-doctor` can sanity-check.
