---
phase: 3
goal: "The agent generates an invoice from Wency's real per-company template and saves it to OneDrive, confirm-before-finalize, with undo."
tasks: 4
waves: 3
---

# Phase 3: Invoice generation from template

**Goal:** The agent reads a credit-note / voyage-summary PDF (already saveable to OneDrive via Phase-2 `save_email_attachment`), LLM-extracts the invoice fields, fills the correct per-company `.docx` template, and uploads the finished invoice to `Verzonden Facturen/{year}` — staged for confirmation (ADR-003) and reversible (undo deletes the generated file).
**Why this phase:** REQ-27, the #1 headline ask from the 2026-06-25 meeting. Wency builds invoices manually in <30s; the agent must be both correct and trusted-only-after-confirm. Phase 2 moved the source PDF into OneDrive; this phase turns it into a sent invoice.

> **Live-drive ground truth** (`@.planning/m6-onedrive-discovery.md`, authoritative): invoices live as fillable `.docx` at `/Documenten/ttt/Bureaublad/alle firma's/Aquavoy Ltd/Verzonden Facturen/{year}`. Two generated formats: **Gefo** voyage/layover (`26-NNN Invoice Aquavoy - Gefo {date} voyage NN.docx`) and **Novo Porto** (`26-NNN Aquavoy Ltd - Novo Porto Scheepvaart BV {date}.docx`). Sequential `YY-NNN` numbering. Gefo `2640xxxx` self-billing statements are INBOUND — out of scope for generation.
>
> **Template nuance (resolved here):** Wency's existing `.docx` are FILLED invoices, not tagged templates. docxtemplater needs `{tag}` placeholders. **Decision: author a clean tagged `.docx` per company as a committed build artifact** under `assets/invoice-templates/` (generated by a committed script from the known 26-047 structure), NOT derived from a filled `.docx` (which has no tags). The `invoice_templates` Supabase table holds per-company metadata (template ref + output folder + field mapping); the runtime fill reads the committed template buffer.
>
> **docxtemplater 3.69.0 + pizzip 3.2.0** (confirmed via docs 2026-06-29): default delimiters are SINGLE braces `{tag}` (matches the discovery tokens). `new PizZip(content)` → `new Docxtemplater(zip, { paragraphLoop: true, linebreaks: true })` → `doc.render(data)` → `doc.toBuffer()` (Buffer; available since 3.62.0). NEW deps — install with `npm i docxtemplater@^3.69.0 pizzip@^3.2.0`.

> **Shared-file coordination with Phase 4 (planned in parallel):** Phase 4 also adds a tool to `onedriveTools.ts`, a case to `executeConfirmedAction.ts`, an undo case to `pendingActions.ts`, and entries to both `REVERSIBLE_TOOLS` sets. Phase 3 scopes its edits to its OWN tool/case/undo only (additive, mirroring the existing `save_email_attachment` pattern). Task 4 below names these shared files so the build-time wave graph serializes Phase 3's and Phase 4's edits to them. Do NOT touch Phase 4's `record_voyage_entry` / `import_voyage_register` surfaces.

---

## Task 1 — Author the tagged per-company `.docx` templates + build script
**Wave:** 1
**Persona:** backend
**Files:**
- `scripts/build-invoice-templates.ts` (new) — Node script that programmatically writes two tagged `.docx` files using `pizzip` + `docxtemplater`'s sibling `docx`-less approach (build a minimal Word XML doc) OR, simpler and what this task specifies, uses the `docx` npm package is NOT added — instead author the two `.docx` by hand as binary fixtures committed to the repo (see Action).
- `assets/invoice-templates/gefo.docx` (new, committed binary) — Gefo voyage/layover invoice template with `{tag}` placeholders.
- `assets/invoice-templates/novo-porto.docx` (new, committed binary) — Novo Porto sales invoice template with `{tag}` placeholders.
- `assets/invoice-templates/README.md` (new) — documents the placeholder token set + the 26-047 source structure + how to regenerate.

**Depends on:** none

**Why:** docxtemplater fills a TAGGED `.docx`; Wency's real files are filled invoices with no tags. We have the full 26-047 structure from the live drive, so we author clean tagged templates now (the discovery doc calls this out as "a build step, not a gate"). Without committed templates there is nothing for Task 2's fill to render.

**Acceptance Criteria:**
- `assets/invoice-templates/gefo.docx` and `novo-porto.docx` exist and open in Word as valid documents.
- Each contains the issuer block (Aquavoy Ltd, Ledras 147 1st floor office 6, 1011 Nicosia Cyprus, VAT CY 60038875Q), a recipient block with `{recipient_name}` / `{recipient_address}` / `{recipient_vat}` tags, `Invoice Date {invoice_date}`, `Invoice number {invoice_number}`, the message line "Hereby we charge you for services rendered onboard of the Mts {vessel}," the line items (Crewing Services `{crewing}`, Travel Cost `{travel}`, Service Fee (Food and Drink) `{service_fee}`, Cash advance `{cash_advance}`, VAT Shifted, Total `{total}`), and the footer (payment term 7 days, Revolut LT62 3250 0781 7194 2284 BIC Revolut21, Admin@aquavoy.com).
- A test render of each template with sample data via docxtemplater succeeds with no "unopened/unclosed tag" error.

**Action:**
1. `npm i docxtemplater@^3.69.0 pizzip@^3.2.0` (NEW deps; pin the major).
2. Author each `.docx`. The reliable, deterministic way without adding a Word-authoring dependency: write `scripts/build-invoice-templates.ts` that constructs a minimal valid `.docx` (a zip with `[Content_Types].xml`, `_rels/.rels`, `word/document.xml`, `word/_rels/document.xml.rels`) using `pizzip`, embedding the issuer/recipient/line-item/footer text from the 26-047 structure with the `{tag}` tokens inline in `word/document.xml`. Build both Gefo and Novo Porto variants (Gefo's message line and filename pattern differ — voyage/layover wording; Novo Porto is the plain sales invoice). Run the script once, commit the two resulting `.docx` + the README. Keep the script committed so the templates are regenerable.
3. Placeholder token set (single-brace, docxtemplater default — do NOT use `{{double}}`): `{recipient_name}`, `{recipient_address}`, `{recipient_vat}`, `{invoice_date}` (DD-MM-YYYY), `{invoice_number}` (YY-NNN), `{vessel}`, `{crewing}`, `{travel}`, `{service_fee}`, `{cash_advance}`, `{total}`.
4. In the script, after writing each file, immediately load it back with `new PizZip(buf)` → `new Docxtemplater(zip, { paragraphLoop: true, linebreaks: true })` → `doc.render({...sampleData})` → `doc.toBuffer()` and assert it does not throw — this proves the tags are well-formed before commit.

**Validation:** (builder self-check)
- `test -f assets/invoice-templates/gefo.docx && test -f assets/invoice-templates/novo-porto.docx && echo EXISTS` → `EXISTS`
- `npx tsx scripts/build-invoice-templates.ts && echo BUILT` → `BUILT` (no "unclosed tag" throw)
- `node -e "const P=require('pizzip'),D=require('docxtemplater'),fs=require('fs');for(const f of ['gefo','novo-porto']){const z=new P(fs.readFileSync('assets/invoice-templates/'+f+'.docx','binary'));const d=new D(z,{paragraphLoop:true,linebreaks:true});d.render({recipient_name:'X',recipient_address:'Y',recipient_vat:'Z',invoice_date:'01-01-2026',invoice_number:'26-001',vessel:'Pride of Faial',crewing:'1',travel:'1',service_fee:'1',cash_advance:'1',total:'1'});d.toBuffer();}console.log('RENDER_OK')"` → `RENDER_OK`

**Context:** Read @.planning/m6-onedrive-discovery.md @.planning/decisions/ADR-007-invoice-templating.md

---

## Task 2 — `invoiceTemplate.ts` (docxtemplater fill adapter) + `invoiceExtraction.ts` (LLM field extraction) + their seam tests
**Wave:** 2
**Persona:** backend
**Files:**
- `src/lib/agents/invoiceTemplate.ts` (new) — exports `fillInvoiceTemplate(templateBuffer: Buffer, data: InvoiceFields): Buffer` and `InvoiceFields` type; pure docxtemplater fill, no IO.
- `src/lib/agents/invoiceExtraction.ts` (new) — exports `extractInvoiceFields(pdfText: string): Promise<ExtractedInvoice>` using the existing `complete()` LLM client, Zod-validated; and the `ExtractedInvoice` Zod schema.
- `src/lib/agents/invoiceTemplate.test.ts` (new) — fills a committed template, asserts non-empty buffer + readable error on a bad template.
- `src/lib/agents/invoiceExtraction.test.ts` (new) — mocks `complete()`, asserts schema validation pass/fail.

**Depends on:** Task 1 (consumes the committed `.docx` templates as test fixtures and defines the field shape the templates expect)

**Why:** ADR-007 §1+§3 — the templating engine and the LLM extraction are the two pure seams (architecture rule 3: adapters at seams). Keeping fill (docx) and extraction (LLM) in their own lib files makes them unit-testable in isolation and keeps `onedriveTools` thin. The schema is load-bearing: a wrong shipper or off-by-one amount must be a typed field the confirm card can show for correction. Each unit ships with its own seam test in the same task so the code and its proof land together.

**Acceptance Criteria:**
- `fillInvoiceTemplate(buf, fields)` returns a non-empty Buffer for a valid template+fields pair and throws a readable error (not a raw docxtemplater multi-error) when a template has a malformed tag.
- `extractInvoiceFields(text)` returns `{ company, recipient_name, recipient_address, recipient_vat, vessel, invoice_date, crewing, travel, service_fee, cash_advance, total, currency }` validated by Zod; `company` is constrained to `"Gefo" | "Novo Porto"`; on a model response that fails schema validation it throws an error naming the invalid fields.
- Missing optional line-item amounts default to `"0.00"` (string, since they render into the docx as-is), never `undefined`, so the rendered invoice never shows a blank `{crewing}`.
- `invoiceTemplate.test.ts`: rendering the committed `gefo.docx` with valid sample fields returns a Buffer with `byteLength > 0`; a hand-crafted bad-tag template throws an Error whose message names the bad tag.
- `invoiceExtraction.test.ts`: a mocked `complete()` returning valid JSON yields a parsed `ExtractedInvoice`; a mocked reply with a missing `total` or an invalid `company` throws the validation error.

**Action:**
1. `invoiceTemplate.ts`: `import Docxtemplater from "docxtemplater"; import PizZip from "pizzip";`. `fillInvoiceTemplate(templateBuffer, data)` → `const zip = new PizZip(templateBuffer); const doc = new Docxtemplater(zip, { paragraphLoop: true, linebreaks: true, nullGetter: () => "" }); doc.render(data); return doc.toBuffer();`. Wrap render in try/catch; on docxtemplater's `error.properties.errors` (its multi-error shape) join the `.message`s into one thrown Error so callers get a readable string. Define and export `InvoiceFields` matching the Task-1 token set.
2. `invoiceExtraction.ts`: define `ExtractedInvoiceSchema = z.object({...})` (`company: z.enum(["Gefo","Novo Porto"])`, the recipient/vessel/date fields as strings, the five amount fields as strings coerced from number-or-string, `currency: z.string().default("EUR")`). `extractInvoiceFields(pdfText)` builds a system+user message instructing strict JSON extraction of those fields from the credit-note/voyage text, calls the existing `complete(messages)` from `@/lib/openrouter/client`, parses the JSON out of the reply (strip code fences), and `ExtractedInvoiceSchema.parse()`s it — on `ZodError` throw `new Error("invoice extraction failed validation: " + issues)`.
3. Do NOT call any OneDrive or DB code in either file — these are pure seams (the IO happens in Task 4's confirm path).
4. `invoiceTemplate.test.ts`: `import { fillInvoiceTemplate } from "./invoiceTemplate"; import { readFileSync } from "fs";` — load `assets/invoice-templates/gefo.docx`, render, assert `byteLength > 0`. For the bad-tag case build a tiny in-memory `.docx` with an unclosed `{tag` (reuse the Task-1 zip scaffold) and assert the thrown message names it.
5. `invoiceExtraction.test.ts`: `vi.mock("@/lib/openrouter/client", () => ({ complete: vi.fn() }))`; set per-case return values (valid JSON → parsed; missing `total` / invalid `company` → ZodError throw); assert.

**Validation:** (builder self-check)
- `npx tsc --noEmit 2>&1 | grep -c "error TS"` → `0`
- `npx vitest run src/lib/agents/invoiceTemplate.test.ts src/lib/agents/invoiceExtraction.test.ts` → all pass
- `grep -c "doc.toBuffer()" src/lib/agents/invoiceTemplate.ts` → `≥ 1`
- `grep -c "ExtractedInvoiceSchema.parse\|.parse(" src/lib/agents/invoiceExtraction.ts` → `≥ 1`

**Context:** Read @.planning/m6-onedrive-discovery.md @.planning/decisions/ADR-007-invoice-templating.md @src/lib/openrouter/client.ts @assets/invoice-templates/README.md

---

## Task 3 — `invoice_templates` migration + Supabase loader/accessor
**Wave:** 2
**Persona:** backend
**Files:**
- `supabase/migrations/0016_invoice_templates.sql` (new) — `invoice_templates(company, template_ref, output_folder_path, field_mapping_json, created_at)`, RLS on / no policies, seed Gefo + Novo Porto rows.
- `src/lib/agents/invoiceTemplates.ts` (new) — exports `getInvoiceTemplate(company: "Gefo" | "Novo Porto"): Promise<InvoiceTemplateRow>` reading from Supabase via the existing service-role client.

**Depends on:** Task 1 (the seed rows reference the committed template paths `assets/invoice-templates/{gefo,novo-porto}.docx` and the field mapping derived from the authored tokens)

**Why:** ADR-007 §4 — per-company template selection is a dynamic Supabase table, not a hardcoded map, so a new company is a row + a file, not a deploy. The migration mirrors `0015_finance_entries.sql` exactly (RLS on, no policies, service-role only — constitution).

**Acceptance Criteria:**
- `0016_invoice_templates.sql` creates the table with RLS enabled and NO policies, a `company` check constraint limiting to `('Gefo','Novo Porto')`, and seeds one row per company whose `template_ref` is the committed asset path and `output_folder_path` is `alle firma's/Aquavoy Ltd/Verzonden Facturen` (year appended at runtime).
- `getInvoiceTemplate("Gefo")` and `getInvoiceTemplate("Novo Porto")` each return the seeded row; an unknown company throws a readable error.
- The migration applies cleanly via the Supabase CLI flow (no hand-edit to prod — constitution).

**Action:**
1. Write `0016_invoice_templates.sql` modeled on `@supabase/migrations/0015_finance_entries.sql`: table columns `id uuid pk default gen_random_uuid()`, `company text not null check (company in ('Gefo','Novo Porto'))` (UNIQUE on company), `template_ref text not null`, `output_folder_path text not null`, `field_mapping_json jsonb`, `created_at timestamptz default now()`. Add a `comment on table` paragraph (ADR-007, service-role only). `alter table ... enable row level security;` with NO policies. Seed both rows with `insert ... on conflict (company) do nothing`.
2. `field_mapping_json` documents the token→source-field mapping for future re-mapping; seed it with the Task-1 token set as an identity map.
3. `src/lib/agents/invoiceTemplates.ts`: use the same `supabaseAdmin()` accessor pattern that `@src/lib/agents/pendingActions.ts` uses; `getInvoiceTemplate(company)` selects `* from invoice_templates where company = $1` single-row; throw if not found.

**Validation:** (builder self-check)
- `test -f supabase/migrations/0016_invoice_templates.sql && echo EXISTS` → `EXISTS`
- `grep -c "enable row level security" supabase/migrations/0016_invoice_templates.sql` → `1`
- `grep -c "create policy\|for select using\|for all using" supabase/migrations/0016_invoice_templates.sql` → `0` (no policies — service-role only)
- `npx tsc --noEmit 2>&1 | grep -c "error TS"` → `0`

**Context:** Read @supabase/migrations/0015_finance_entries.sql @supabase/migrations/0010_pending_actions.sql @src/lib/agents/pendingActions.ts

---

## Task 4 — `generate_invoice_from_template` tool: stage + execute + undo + REVERSIBLE_TOOLS + system prompt + confirm-path test
**Wave:** 3
**Persona:** backend
**Files:**
- `src/lib/agents/onedriveTools.ts` — add `generate_invoice_from_template` to `TOOL_DEFINITIONS`, to the `DESTRUCTIVE` set (~line 992), a `summarizeAction` case (~line 1028), and a dedicated stage branch inside the `DESTRUCTIVE` block (mirror the `save_email_attachment` branch ~line 1133). EDIT ONLY this tool's additions.
- `src/lib/agents/executeConfirmedAction.ts` — add `case "generate_invoice_from_template"` (mirror the `save_email_attachment` case ~line 272). EDIT ONLY this case.
- `src/lib/agents/pendingActions.ts` — add `case "generate_invoice_from_template"` to `undoAction`'s switch (mirror the `save_email_attachment` delete case ~line 357). EDIT ONLY this case.
- `src/app/page.tsx` — add `"generate_invoice_from_template"` to `REVERSIBLE_TOOLS` (~line 62). One-line add.
- `src/app/finance/page.tsx` — add `"generate_invoice_from_template"` to `REVERSIBLE_TOOLS` (~line 61). One-line add.
- `src/lib/openrouter/client.ts` — add a `SYSTEM_PROMPT` paragraph (after 5e, ~line 197) describing the tool.
- `src/lib/agents/executeConfirmedAction.test.ts` — ADD a `generate_invoice_from_template` block (mocks already exist for onedrive `uploadFile`; add a mock for `getInvoiceTemplate` + `fillInvoiceTemplate` + `readFileSync`).

**Depends on:** Task 2 (imports `fillInvoiceTemplate` + `extractInvoiceFields`), Task 3 (imports `getInvoiceTemplate`)

**Why:** ADR-003 + ADR-007 §5 — the write to OneDrive must be confirm-before-write: stage captures the extracted fields + chosen company template + target path; the docx fill + `uploadFile` run ONLY at confirm; undo deletes the generated file. This is the enforcement (the model has no code path to the side-effect). The confirm-path seam test lands in the same task that wires the confirm path, so the proof that `uploadFile` gets the year-suffixed path and `undo_data` captures the uploaded id ships with the code it tests. **Shared-file note:** Phase 4 edits these same files for `record_voyage_entry`/`import_voyage_register`; this task adds ONLY the `generate_invoice_from_template` surface so the two phases' edits stay non-overlapping at build time.

**Acceptance Criteria:**
- Calling `generate_invoice_from_template` in the agent loop returns `{ status: "confirmation_required", action_id, summary }` and uploads NOTHING (stage only); fails closed with "no verified principal in session" when `sessionPrincipal` is absent (mirror existing gate).
- The staged `summary` is human-readable, e.g. `Generate Novo Porto invoice 26-027 for Novo Porto Scheepvaart BV → Verzonden Facturen/2026`.
- On confirm: `getInvoiceTemplate(company)` selects the template → its committed `.docx` buffer is read → `fillInvoiceTemplate` produces the buffer → `uploadFile(connId, { path: "alle firma's/Aquavoy Ltd/Verzonden Facturen/{year}" }, "{invoice_number} ...docx", buffer, docxContentType)` uploads it → returns `{ itemId, name, webUrl }` and `undo_data: { uploadedItemId }`.
- Undo deletes the uploaded invoice via `deleteItemOnDrive`; the tool appears as reversible in both chat and finance pending-action cards.
- The system prompt paragraph instructs the agent to call the tool ONCE, relay the summary, and not re-call after the user says yes (mirror the 5e wording).
- `executeConfirmedAction.test.ts`: the new block mocks `getInvoiceTemplate`/`fillInvoiceTemplate`/`uploadFile`/`readFileSync`, calls `executeConfirmedAction("generate_invoice_from_template", fields, "Wency")`, and asserts `uploadFile` was called with the year-suffixed path and that `undo_data.uploadedItemId` equals the uploaded item id; the full suite stays green (no regression).

**Action:**
1. `TOOL_DEFINITIONS`: add the function with params `company` (enum Gefo/Novo Porto — optional; if omitted the agent should infer from the source doc, but the staged fields are user-correctable), the extracted fields (`recipient_name`, `recipient_address`, `recipient_vat`, `vessel`, `invoice_date`, `invoice_number`, `crewing`, `travel`, `service_fee`, `cash_advance`, `total`, `currency`), and `targetYear` (defaults to current year). Description mirrors `save_email_attachment`'s "CONFIRMED BEFORE SAVING / call ONCE / relay summary" framing.
2. Add `"generate_invoice_from_template"` to the `DESTRUCTIVE` set with a comment (ADR-007 confirm-before-write; fill+upload at confirm, never in the loop).
3. Add a stage branch (mirror `save_email_attachment` ~1133): validate `invoice_number` + `company` present; reject `targetFolderPath`-style `..` traversal is N/A here (path is fixed), but DO validate `company` is one of the two; build the summary; `stagePendingAction({ principal: sessionPrincipal, tool: "generate_invoice_from_template", args: {...all fields, targetYear}, summary })`; return `confirmation_required`.
4. Add `summarizeAction` case producing the readable summary above.
5. `executeConfirmedAction.ts` `case "generate_invoice_from_template"`: import `getInvoiceTemplate` from `@/lib/agents/invoiceTemplates`, `fillInvoiceTemplate` from `@/lib/agents/invoiceTemplate`, `readFileSync` to load the committed template at `template_ref`. Resolve company → row → read template buffer → `fillInvoiceTemplate(buf, fields)` → `resolveConnectionId()` → `uploadFile(connId, { path: \`${row.output_folder_path}/${year}\` }, filename, buffer, "application/vnd.openxmlformats-officedocument.wordprocessingml.document")` → return `{ result: { generated: true, itemId, name, webUrl }, undo_data: { uploadedItemId: item.id } }`. Filename follows the per-company convention (Gefo: `{number} Invoice Aquavoy - Gefo {date} voyage NN.docx`; Novo Porto: `{number} Aquavoy Ltd - Novo Porto Scheepvaart BV {date}.docx`).
6. `pendingActions.ts` undo `case "generate_invoice_from_template"`: identical to the `save_email_attachment` delete case — read `undo.uploadedItemId`, `resolveConnectionId()`, `deleteItemOnDrive(connId, uploadedItemId)`. (Combine with the existing `save_email_attachment` case via fall-through if both reduce to the same delete, to keep it DRY — both reverse by deleting an uploaded item.)
7. Add `"generate_invoice_from_template"` to both `REVERSIBLE_TOOLS` sets.
8. `SYSTEM_PROMPT`: add a `5f. GENERATE INVOICES` paragraph after 5e mirroring its structure (read the source PDF with read_file → extract fields → call generate_invoice_from_template ONCE → staged for confirmation → reversible → relay summary, do not re-call).
9. `executeConfirmedAction.test.ts`: mirror the existing `save_email_attachment` test — add `vi.mock("@/lib/agents/invoiceTemplates", ...)`, `vi.mock("@/lib/agents/invoiceTemplate", ...)`, and a `fs` `readFileSync` mock; call `executeConfirmedAction("generate_invoice_from_template", fields, "Wency")`; assert the `uploadFile` call args include the year-suffixed `Verzonden Facturen/{year}` path and that `undo_data.uploadedItemId` equals the uploaded item id.

**Validation:** (builder self-check)
- `npx tsc --noEmit 2>&1 | grep -c "error TS"` → `0`
- `grep -c "generate_invoice_from_template" src/lib/agents/onedriveTools.ts` → `≥ 4` (TOOL_DEFINITIONS, DESTRUCTIVE, summarizeAction, stage branch)
- `grep -c "generate_invoice_from_template" src/lib/agents/executeConfirmedAction.ts` → `≥ 1`
- `grep -c "generate_invoice_from_template" src/lib/agents/pendingActions.ts` → `≥ 1`
- `grep -c "generate_invoice_from_template" src/app/page.tsx src/app/finance/page.tsx` → each `1`
- `grep -c "generate_invoice_from_template\|GENERATE INVOICE" src/lib/openrouter/client.ts` → `≥ 1`
- `npx vitest run src/lib/agents/executeConfirmedAction.test.ts` → all pass
- `npx vitest run` → full suite green (no regression)

**Design:**
- Register: product
- Tokens used: var(--accent), var(--surface), var(--border), var(--text) (constant-only edit to REVERSIBLE_TOOLS — no new rendered elements; tokens are those the existing pending-action card already uses)
- Scope: component
- Anti-pattern guard: builder runs `node bin/slop-detect.mjs` pre-commit (no-op — no style changes)

**Context:** Read @src/lib/agents/onedriveTools.ts @src/lib/agents/executeConfirmedAction.ts @src/lib/agents/pendingActions.ts @src/lib/openrouter/client.ts @src/lib/agents/invoiceTemplate.ts @src/lib/agents/invoiceExtraction.ts @src/lib/agents/invoiceTemplates.ts @src/lib/agents/executeConfirmedAction.test.ts

---

## Success Criteria
- [ ] Given a credit-note/voyage PDF's text, `extractInvoiceFields` returns `{ company, recipient_*, vessel, invoice_date, invoice_number, line-item amounts, currency }`, Zod-validated (criterion: Roadmap P3 #1).
- [ ] `generate_invoice_from_template` stages a pending action showing the extracted fields + matched company template and does NOT execute until confirmed (criterion: P3 #2, ADR-003).
- [ ] On confirm, the correct per-company `.docx` template (Gefo vs Novo Porto, selected from `invoice_templates`) is filled via docxtemplater and uploaded to `Verzonden Facturen/{year}`; undo deletes the generated file (criterion: P3 #3).
- [ ] E2E path holds: read PDF → extract → stage → confirm → fill → upload, proven by the seam tests on a sample (criterion: P3 #4).
- [ ] No regression in the existing tool/confirm surface (`npx vitest run` green; `npx tsc --noEmit` clean).

## Verification Contract

### Contract for Task 1 — templates exist
**Check type:** file-exists
**Command:** `test -f assets/invoice-templates/gefo.docx && test -f assets/invoice-templates/novo-porto.docx && echo EXISTS`
**Expected:** `EXISTS`
**Fail if:** Either template `.docx` is missing.

### Contract for Task 1 — templates render without tag errors
**Check type:** command-exit
**Command:** `node -e "const P=require('pizzip'),D=require('docxtemplater'),fs=require('fs');for(const f of ['gefo','novo-porto']){const z=new P(fs.readFileSync('assets/invoice-templates/'+f+'.docx','binary'));const d=new D(z,{paragraphLoop:true,linebreaks:true});d.render({recipient_name:'X',recipient_address:'Y',recipient_vat:'Z',invoice_date:'01-01-2026',invoice_number:'26-001',vessel:'V',crewing:'1',travel:'1',service_fee:'1',cash_advance:'1',total:'1'});d.toBuffer();}console.log('RENDER_OK')" 2>&1 | tail -1`
**Expected:** `RENDER_OK`
**Fail if:** A docxtemplater "unopened/unclosed tag" error is thrown — the templates have malformed placeholders.

### Contract for Task 1 — deps installed
**Check type:** grep-match
**Command:** `grep -c "docxtemplater\|pizzip" package.json`
**Expected:** Non-zero (≥ 2)
**Fail if:** `npm i docxtemplater pizzip` was not run.

### Contract for Task 2 — fill adapter wired to docxtemplater
**Check type:** grep-match
**Command:** `grep -c "doc.toBuffer()" src/lib/agents/invoiceTemplate.ts`
**Expected:** Non-zero (≥ 1)
**Fail if:** Returns 0 — the fill does not produce a buffer via the confirmed 3.69.0 API.

### Contract for Task 2 — extraction is schema-validated
**Check type:** grep-match
**Command:** `grep -c "z.enum(\[\"Gefo\"\|.parse(" src/lib/agents/invoiceExtraction.ts`
**Expected:** Non-zero (≥ 1)
**Fail if:** Returns 0 — extraction is not Zod-validated (an unchecked LLM parse could book a wrong invoice).

### Contract for Task 2 — seam tests pass
**Check type:** command-exit
**Command:** `npx vitest run src/lib/agents/invoiceTemplate.test.ts src/lib/agents/invoiceExtraction.test.ts 2>&1 | tail -3`
**Expected:** Both files report passed (0 failed).
**Fail if:** Either fill or extraction test fails — the adapter or schema is broken.

### Contract for Task 3 — migration is service-role-only (RLS, no policies)
**Check type:** command-exit
**Command:** `grep -c "enable row level security" supabase/migrations/0016_invoice_templates.sql; grep -c "create policy" supabase/migrations/0016_invoice_templates.sql`
**Expected:** First line `1`, second line `0`
**Fail if:** RLS not enabled, or any policy exists (would expose the table to anon/authenticated — constitution violation).

### Contract for Task 3 — both companies seeded + accessor exists
**Check type:** grep-match
**Command:** `grep -c "Gefo\|Novo Porto" supabase/migrations/0016_invoice_templates.sql; grep -c "getInvoiceTemplate" src/lib/agents/invoiceTemplates.ts`
**Expected:** Both lines non-zero
**Fail if:** A company seed row or the accessor is missing.

### Contract for Task 4 — tool staged, not executed in loop
**Check type:** grep-match
**Command:** `grep -c "generate_invoice_from_template" src/lib/agents/onedriveTools.ts`
**Expected:** Non-zero (≥ 4 — TOOL_DEFINITIONS, DESTRUCTIVE set, summarizeAction, stage branch)
**Fail if:** < 4 — the tool is not fully gated as confirm-before-write.

### Contract for Task 4 — confirm path + undo wired
**Check type:** grep-match
**Command:** `grep -c "generate_invoice_from_template" src/lib/agents/executeConfirmedAction.ts src/lib/agents/pendingActions.ts`
**Expected:** Both files non-zero
**Fail if:** The execute case or the undo case is missing — the tool would stage but never fill/upload, or could not be undone.

### Contract for Task 4 — fill adapter wired into the confirm path
**Check type:** grep-match
**Command:** `grep -c "fillInvoiceTemplate" src/lib/agents/executeConfirmedAction.ts`
**Expected:** Non-zero (≥ 1)
**Fail if:** Returns 0 — `fillInvoiceTemplate` exists in lib but is never called from the confirm path (the docx would never be filled).

### Contract for Task 4 — template accessor wired into the confirm path
**Check type:** grep-match
**Command:** `grep -c "getInvoiceTemplate" src/lib/agents/executeConfirmedAction.ts`
**Expected:** Non-zero (≥ 1)
**Fail if:** Returns 0 — `getInvoiceTemplate` exists in lib but is never called from the confirm path (the per-company template would never be selected).

### Contract for Task 4 — extraction wired into the agent path
**Check type:** grep-match
**Command:** `grep -rc "extractInvoiceFields" src/lib/agents/onedriveTools.ts src/lib/agents/executeConfirmedAction.ts`
**Expected:** At least one of the two files returns non-zero (the file whose branch extracts the invoice fields)
**Fail if:** Both return 0 — `extractInvoiceFields` exists in lib but is never called (the LLM extraction is dead code).

### Contract for Task 4 — reversible in both UI surfaces
**Check type:** command-exit
**Command:** `grep -c "generate_invoice_from_template" src/app/page.tsx; grep -c "generate_invoice_from_template" src/app/finance/page.tsx`
**Expected:** Each line `1`
**Fail if:** Missing from either `REVERSIBLE_TOOLS` — the Undo control would not render on that page.

### Contract for Task 4 — TypeScript compiles
**Check type:** command-exit
**Command:** `npx tsc --noEmit 2>&1 | grep -c "error TS"`
**Expected:** `0`
**Fail if:** Any TS error.

### Contract for Task 4 — confirm-path test + full suite pass
**Check type:** command-exit
**Command:** `npx vitest run 2>&1 | tail -3`
**Expected:** Suite reports all files passed (0 failed).
**Fail if:** Any test fails — the fill, extraction, or confirm path is broken, or a regression was introduced.

### Contract for Task 4 — E2E confirm path proven
**Check type:** behavioral
**Command:** (verifier inspects `executeConfirmedAction.test.ts` `generate_invoice_from_template` block)
**Expected:** The test calls `executeConfirmedAction("generate_invoice_from_template", fields, "Wency")` with `uploadFile` mocked, and asserts `uploadFile` received the year-suffixed `Verzonden Facturen/{year}` path and that `undo_data.uploadedItemId` matches the uploaded id.
**Fail if:** No assertion on the upload path or the undo capture — the E2E contract (P3 #4) is unproven.
